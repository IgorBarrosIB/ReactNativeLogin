import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: 'red',
    borderRadius: 10,
    padding: 20,
    marginTop: 10

  },
  text: {
    color: 'white',
    fontSize: 18,
    textAlign: 'center',
  }
});